{
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy",
    "deploy:prod": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/functions": "^3.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.1",
    "typescript": "^5.5.0"
  }
}
